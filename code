#include <iostream>

using namespace std;

int main ()
{
  int v[1501], n, p;
    
  cin >> n >> p;
    
  for (int i = 1; i <= n; i++)
  {
      cin >> v[i];
      
      if (i != p)
      {
          cout << v[i] << " ";
      }
  }
    
  return 0;
}
